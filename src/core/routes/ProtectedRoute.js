import React from "react";
import { Redirect } from "react-router";

const ProtectedRoute = ({ condition, component, redirectPathname }) => {
  //   const Component = component;
  //   return condition ? <Component /> : <Redirect to={redirectPathname} />;
};

export default ProtectedRoute;
